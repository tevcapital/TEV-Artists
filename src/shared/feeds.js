// feeds.js — TEV Artists news feed map
// TEV Artists does not use news feeds. The FEEDS map is intentionally empty.
// The engine's feed-fetching code remains unchanged and will inject nothing
// into prompts for any agent.

const FEEDS = {};

module.exports = { FEEDS };
